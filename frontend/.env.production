# Frontend production environment for cPanel
# The Next.js app proxies /api/* to this backend port internally
BACKEND_PORT=3001

# Public API URL (used if you need absolute URLs client-side)
NEXT_PUBLIC_API_URL=https://argusshipping.co
